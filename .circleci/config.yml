# Couldn't automatically generate a config from your source code.
# This is a generic template to serve as a base for your custom config
# See: https://circleci.com/docs/configuration-reference
# Stacks detected: cicd:github-actions:.github/workflows
version: 2.1

executors:
  pactfoundation:
    docker:
      - image: pactfoundation/pact-cli
        environment:
          PACT_BROKER_BASE_URL: https://beso.pactflow.io
          PACT_BROKER_TOKEN: a0txy_Q79oE5-EDUSMvr_g

jobs:
  test:
    docker:
      - image: mcr.microsoft.com/dotnet/sdk:8.0
    environment:
      PACKAGE_VERSION: 1.3.<< pipeline.number >>
    steps:
      - checkout
      - run:
          name: Test
          command: |
            dotnet test tests

  pactflow:
    executor: pactfoundation
    steps:
      - run:
          name: publishhpact
          command: |
            PACT_CLI="pact broker publish --auto-detect-version-properties --to-environment production"
            ${PACT_CLI}


workflows:
  example:
    jobs:
      - test:
          name: tests
      - pactflow:
          name: publishhpact
          requires:
            - tests
